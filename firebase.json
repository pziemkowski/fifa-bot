{
  "database": {
    "rules": "database.rules.json"
  },
  "functions": {
    "source": "functions",
    "predeploy": "npm --prefix functions run build",
    "postdeploy": "npm --prefix functions run clean"
  }
}
